<% if notice || alert %>
  <p style="font-size: 0.9em; text-align: center; margin: 0.2em 0; position: absolute; top: 5px; left: 0px;">
    &nbsp;
    <span class="notice"><%= notice %></span>
    <span class="alert"><%= alert %></span>
    &nbsp;
  </p>
<% end %>
<nav>
  <%= render 'shared/menu' %>
</nav>

<main>
  <div class="login-container", style="top: 100px; bottom: 100px;">
    <div class="login-pane">
      <h1 style="text-align: center;">Widget Store</h1>
    </div>
  </div>
</main>

<!--
<table>
  <thead>
    <tr>
      <th>Widget name</th>
      <th>Logo url</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @widgets.each do |widget| %>
      <tr>
        <td><%= widget.widget_name %></td>
        <td><%= widget.logo_url %></td>
        <td><%= widget.description %></td>
        <td><%= link_to 'Show', widget %></td>
        <td><%= link_to 'Edit', edit_widget_path(widget) %></td>
        <td><%= link_to 'Destroy', widget, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Widget', new_widget_path %>
-->
