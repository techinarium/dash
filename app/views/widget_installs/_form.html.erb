<%= form_with(model: widget_install, local: true) do |form| %>
  <% if widget_install.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(widget_install.errors.count, "error") %> prohibited this widget_install from being saved:</h2>

      <ul>
      <% widget_install.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :widget_install_user_id %>
  </div>

  <div class="field">
    <%= form.label :widget_id %>
    <%= form.text_field :widget_id, id: :widget_install_widget_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
