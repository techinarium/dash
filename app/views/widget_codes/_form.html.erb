<%= form_with(model: widget_code, local: true) do |form| %>
  <% if widget_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(widget_code.errors.count, "error") %> prohibited this widget_code from being saved:</h2>

      <ul>
      <% widget_code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :widget_code %>
    <%= form.text_area :widget_code, id: :widget_code_widget_code %>
  </div>

  <div class="field">
    <%= form.label :version %>
    <%= form.text_field :version, id: :widget_code_version %>
  </div>

  <div class="field">
    <%= form.label :widget_id %>
    <%= form.text_field :widget_id, id: :widget_code_widget_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
